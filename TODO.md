# TODO
- [x] (Plan step 1) Unify card styling in `style.css` for .about-card, .instructor-card, .tcard (radius/shadow/hover + consistent padding + equal-height behavior)
- [x] (Plan step 2) Reduce “Quem somos” card height on mobile by adjusting .about-grid/.about-card mobile rules (remove excessive centering/height)
- [x] (Plan step 3) Update “Professoras” to show 2 columns on mobile by editing inline CSS in `index.html` (.instructors-grid)
- [x] (Plan step 4) Redesign testimonials: mobile layout + typography + equal heights by editing inline CSS in `index.html` and (if needed) shared rules in `style.css`
- [ ] (Plan step 5) Final mobile polish: image sizing, section spacing, and card hover/active consistency below 768px

- [ ] Manual verification: open index.html on mobile viewport (<768px) and compare all 4 requirements

